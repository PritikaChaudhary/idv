<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
  	<% if msg != "You are already signed in."%>
    	<%= content_tag :div, msg, :class => "flash_#{name}" %>
    <% end %>
  <% end %>
<% end %>
